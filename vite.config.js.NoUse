// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    rollupOptions: {
      external: ['/wasm/slrm_module.js'],
    },
    lib: {
      entry: 'workers/worker.js',
      formats: ['es'],
      fileName: () => 'worker.js',
    },
    outDir: 'dist',
    emptyOutDir: false,
  },
});
